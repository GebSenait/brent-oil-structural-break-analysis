# Notebooks

- **Exploratory analysis**: Visualizations, summary stats, time series diagnostics (trend, stationarity, volatility).
- **Task-1**: Foundation workflows (data load, event merge, diagnostic checks). Outputs feed into `src/` and `docs/`.
- **Future**: Bayesian change point runs, scenario analysis, dashboard-backed analyses.
- Run in order where dependencies exist; document kernel and package versions in README or first cell.
